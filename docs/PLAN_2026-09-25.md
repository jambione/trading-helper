# Plan — tonight (Thu 9/24) and Fri 2026-09-25

Rule: nothing goes live tomorrow unless it passed the replay tonight. Big unproven pieces run in shadow first.

Goals (Jonathan, updated 13:37): positions open constantly through the day, several at the same time, producing a small, consistent profit. Everything else serves that: a full, fresh book of quality names (Movers, Trending, Research) that are ready to arm and seated before the fast %R −50 cross, at least one open per ~10 minutes, exits that capture profit, and a simple system (replace, don't stack).

## Tonight, in order
1. 16:02 reverts: ai_watch_max_sip_spread_pct 0.30→0.2, ai_watch_decision_max_age_sec 60→15, ai_stale_data_max_age_sec 60→15, ai_watch_dead_seat_evict_sec 90→30 (master-mac, push, mini pull, no restart).
2. Replay harness on today's snapshot archive (~/session_snapshots/2026-09-24 on the mini).
3. Session recorder: every price update with source+ts, quote failures/429s, source nominations, config fp.
4. Ticker-list leak (src=book rows never expire) + batched dashboard quotes.
5. One price, one clock: fresher of engine live_print and dashboard quote; arm, evict, flatten read one age; 429 is not "no data"; keep −50 cross state across evictions.
6. Merge replay-tool: volume clock, armable-only book (ai_watch_admit_arm_gates=true), $20–$100 band for every source; ai_watch_movers_min_rvol=1.0.
7. Book server with runway ranking (runway score + closeness to −50, refill from ranked queue). Shadow tomorrow unless it clearly passes replay tonight.
8. Exits unchanged tonight (entries and name quality first). Ratchet study result is read and filed for a later exit session.
9. Deploy: tests, replay 09-21..09-24 vs pass bar, commit, push, pull on mini, launchctl kickstart; verify agy_auth=ok, dashboard [SNAP] rebuilt, signal_state.json fresh.

## Pass bar (in priority order)
1. **Concurrency (headline):** share of the session (09:40–15:50) with >= 1 position open and with >= 2 open; also avg and max open. Proposed targets: >= 1 open for >= 80% of the session, >= 2 open for >= 50%. Stay within the current max positions and free-equity sizing; never raise the limit to hit this.
2. **Small, consistent profit:** per replay day net P/L >= 0; P/L per trade (R) must not get worse than baseline, so extra concurrency cannot come from worse trades; report worst day and worst hour.
3. **Book health (supports 1 and 2):** book >= 10 by 09:40 · >= 6 armable · data-blocked < 10% · every arm reaches an order · >= 1 open per 10 min.
4. **Simplicity:** settings count and admit/seat-path count must not grow; the book server retires the old intake paths it replaces.

## Fri morning
- ~08:00 pre-open check: mini == tip, config live, processes up, recorder writing, shadow on.
- 09:30–10:30 watch the pass bar live; compare shadow book vs live.
- Rest of session: observe/score. Config-only fixes for unintended blockers only.

## Fri evening
- Replay Friday's full recording, old vs new logic.
- Promote book server to live for Monday if shadow passes.
- Exit decision from ratchet study, only after the entry/book changes are proven.

## Deliberately out
Arm changes (−50 cross stays), new hard gates, premarket rebuild, deleting dead square/MACD code.

References: docs/HOLISTIC_BOOK_REVIEW_2026-09-24.md, docs/RUNWAY_STUDY_2026-09-24.md, docs/HANDOFF_2026-09-24.md (replay-tool).
