# Handoff — 2026-09-26 (weekend stop; resume Tuesday)

## Live on the mini (restarted Sat 11:27, `master-mac` f273220 — barebones SHIPPED)
- **Trading:** %R -50 cross arm at 0.06R, $20 floor, 5 max open, 1000R paper brake, momentum spread exemption still on.
- **NEW Sat (barebones merged):**
  - a young IEX quote agreeing with a recent print counts as a fresh price;
  - the price clock is stamped at measurement time, not poll start;
  - retired arms and 25 dead settings are removed (verified neutral: the 9/25 replay gave identical trades before and after).
  - Expect fewer `tape_only` refusals and more opens on Monday.
- **The desk_io recorder is new:** every external read of ai_trader (Alpaca REST, the /api/state slice, input JSON files), a boot marker, config at boot, and per-poll decision rows. Files: `ai_reports/sessions/DAY/{wire,decisions,config}.jsonl.gz`.
- **The nightly job is new** (`tools/nightly.py`, started by session_snapshot at 16:05). It runs the fidelity replay, then the exact acceptance replay, and writes `ai_reports/nightly/DAY/summary.md`. Saturday's real recording replayed with 12,491 reads served and 0 misses (no decisions off-session).

## Tuesday, first
1. Read `ai_reports/nightly/2026-09-28/summary.md` on the mini. PASS requires 0 misses, >=99% decision agreement and >=95% buys within 5 s. If it FAILs, the first-divergence list says where; fix the harness before trusting any replay.
2. Compare Monday live to Friday: opens per 10 min, % time >=1 and >=2 open, tape_only share of arm checks, net bp after spread. If Monday is worse, roll back the freshness rule with `ai_watch_quote_freshness=false` in config/bot_config.json (needs a restart).

## What barebones contained (merged as f273220; suite green on macOS: 3593 passed)
- **Freshness:** a young IEX quote (<=5 s) that agrees within 30 bp with a print <=60 s old counts as a fresh price (`cross_checked_quote`, `price_src_fresh`; rollback `ai_watch_quote_freshness=false`). Against SIP it cleared 60% (9/25) and 28% (9/24) of false stale refusals, with p90 error 3-5 bp. Price-clock stamping fix (e79fcce, from the other session).
- **Deletions** (cloud session): square/triangle/pre-square arms and legacy_arms.py, the MACD gap arm and gates, the CM RSI gates and their vetoes, prefer-square eviction, and 2 unread keys. Settings went from 615 to 590. Five keys were off live but on in config.py defaults; the defaults were aligned to live, then deleted. The `ai_watch_exh_mid_rise_arm` default is still off, which only matters with no bot_config.json.

## Findings that should steer the next week
- **No edge found:** on free data, 30-min holds in $20-100 names have about 0 gross edge. On 60 days, net = -spread in every spread bucket.
  - The combined spread+strength+%R score (+6.6 bp over 8 days) failed on 60 days (-11.2 vs random -10.4).
  - %R triggers match random minutes.
  - A volume breakout helps only on SIP volume (paid).
  - See memories: combined-score…, wr-triggers…, needle-study….
- **Barebones buys opens and testability, not profit.**
  - Stale-price refusals were 43% of arm checks.
  - About half of the desk's stale verdicts had a fresh IEX print it missed.
  - Monday's wire/decision recording will show where those prints are lost (dashboard fetch versus the desk's age bookkeeping).
- **The open strategic question:** holding period, universe, data or strategy. Decide it with a fresh budget.

## Housekeeping
- **Remote branches to delete** (all merged; blocked by the permission check, run by hand): `git push origin --delete barebones-delete-retired barebones-freshness feat/fractional-shares-rth feat/rstop-arm-at-last fix/dual-leave-ob-triangle master-mac-level2-update replay-tool runway-study`
- **Test hygiene bug:** a test rewrites the tracked `config/bot_config.json` with full defaults during a suite run (seen 9/26). Never run the suite in the mini's live checkout until this is fixed.
- **Code-only work** goes to cloud sessions via `docs/briefs/*.md` until the credits expire on 11/05.

## For another assistant taking over (e.g. Grok) — read this first
- **Machines:** the MacBook is edit/git only. The live paper desk runs on the Mac mini: `ssh mac-mini-away` (Tailscale), repo `~/repo/trading-helper`, python `.venv/bin/python`, live settings in `config/bot_config.json`. Its state files are on the mini only.
- **Restart the desk ONLY with** `launchctl kickstart -k gui/$(id -u)/com.jambi.trading-desk` (run on the mini). Never `./trading restart` over ssh: it loses Keychain access and breaks research auth. After a restart, check `logs/ai_trader.log` for `desk_io recording` and `agy_auth=ok`.
- **Deploy** = commit + push on the MacBook, `git pull --ff-only` on the mini, then the restart above. The mini cannot git push.
- **Never:**
  - commit secrets (the repo is PUBLIC);
  - switch the live feed to SIP, or pay for data;
  - change trading behaviour during market hours (09:30-16:00 ET) without the operator's OK;
  - run the pytest suite inside the mini's live checkout (a test rewrites config/bot_config.json).
- **Don't re-litigate these without new evidence:**
  - Exits cannot move the mean.
  - %R triggers match random minutes on runway.
  - On 60 days, gross is ~0 in every spread bucket.
  - The docs/ and memory findings above are the record.
- **Monday's verdict:** `ai_reports/nightly/2026-09-28/summary.md` on the mini, written ~16:05-18:00 ET.
- **Studies:** `tools/studies/`. Replays: `tools/replay_session.py` (`--exact` for recorded days; run on the mini after hours, at most 2 at a time, `nice -n 15`).
