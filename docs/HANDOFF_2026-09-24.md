# Handoff — 2026-09-24 (written 12:15 ET by Claude)

You are picking up a live trading-desk project mid-session. Read all of this before acting.

## The project
- Repo: `~/repo/trading-helper` (GitHub `jambione/trading-helper`, **public**; never commit secrets).
  - Dev machine: the MacBook. Live desk: a Mac mini reached as `ssh mac-mini-away` (Tailscale), same repo path.
- The live desk trades an **Alpaca paper** account (margin, ~$2,265 equity).
  - Always pipe ssh commands with `< /dev/null`.
- Processes on the mini:
  - `dashboard.py` (port 8888)
  - `signal_engine.py`
  - `ai_trader.py`
  - `movers_screener.py`
  - `trending_screener.py`
  - `tools/watchdog.py`
- The user (Jonathan) judges progress by four pillars:
  1. opportunities all day, meaning a full book;
  2. quality entries;
  3. runway for opens;
  4. exits that capture profit.
- Current goals: **a book full of names queued at the arming point, and multiple concurrent positions all day.**

## Rules the user has set
1. **No money spent** on data or services (no paid real-time SIP) until the desk shows consistent profit. Design for the free feeds:
   - IEX and Finnhub print $20–$100 names only every 5–45 seconds.
   - SIP is served only 15+ minutes late.
   - One shared Alpaca rate limit, about 200 requests/minute.
2. **Mid-session:** ask before any code change or restart.
   - Config-only changes to `config/bot_config.json` are read live with no restart.
   - The user gave standing approval today for temporary config changes that fix *unintended* blockers. Never loosen an intended gate.
   - Intended gates: the −50 cross wait, gap-down, real wide spread, the $20/$100 band, cooldowns, the loss brake.
3. **"Ship it" means deploy and restart:** commit, push, pull on the mini, AND restart.
   - Restart: `ssh mac-mini-away 'launchctl kickstart -k gui/$(id -u)/com.jambi.trading-desk' < /dev/null`.
   - `MINI_SSH=mac-mini-away ./scripts/deploy_mini.sh --no-push` does the same, but refuses when the mini's tree is dirty. The desk rewrites the tracked `valid_tickers.txt`, which trips that guard; use `git pull --ff-only` plus kickstart instead.
   - Don't run `claude /login` over ssh. Don't start desk processes over ssh (they orphan and block the LaunchAgent).
4. **Evidence before changes:** test on bars or the replay first. One day's P/L is noise.
5. **Commit messages** end with `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>` (or your own attribution).

## What happened today (so the logs make sense)
- **09:30–10:15:** the book held 2–5 names, 0–1 of them buyable. Two causes:
  - Movers rvol was paced against the live clock but read 15-minute-late SIP volume, so it came out 3–4× low and dropped PFE, KR, VZ and similar names.
  - The spread gate was reading premarket spreads.
- **10:17:** CRWV armed and was cancelled by `confirm_slip` over a 12-cent move in our favor. Fixed by config: `ai_entry_confirm_max_slip_px` set to 0.
- **10:20–10:47:** the dashboard and engine froze. Root cause, found with py-spy: a Finnhub deadlock. `_drain_pending` held `FINNHUB_STATE.lock` and called `add_log`, which takes the same lock. **Fixed in `9a0acf0`** (the lock is now an RLock). The Finnhub cap is back at 50 in `a10b538`.
- **11:05:** RKLB bought, then sold at 11:12 for −$2.88 by the **stale-data guard** (15s), not by its stop. Thin prints plus an Alpaca 429 on the REST fallback.
- **CDNA** was evicted 3 times as a "dead seat" and missed its whole move. The book's price clock is the dashboard feed, while the engine had CDNA live.
- **Trades by 12:17:**
  - RKLB −$2.88 (stale_data: wrong sale; the guard was raised to 60s after)
  - VNOM −$4.10 (local stop; never reached +0.3%)
  - AR −$4.65 (local stop at $35.90; the stop did its job before a big red candle)
  - RKLB −$1.30 (left_overbought)
  - Open at 12:17: RKLB (entered $72.73) and **INFQ at $14.01, UNDER the $20 floor**
- **INFQ** came in through the research (agy) path, which skips the price band, the same hole as CLF and NBIS. It has its normal stop. The `admit_arm_gates` fix on `replay-tool` closes the hole for every source.
- **`buy_cap` skip:** RKLB armed at 12:16:19 and was skipped `buy_cap`, then filled on the next arm at 12:16:32. Check what `buy_cap` counts (see ai_entry_watch.py around line 16560).

## Live temporary config (mini `config/bot_config.json`, afternoon only)
| key | now | revert to | when |
|---|---|---|---|
| ai_watch_max_sip_spread_pct | 0.30 | 0.2 | 16:02 |
| ai_watch_decision_max_age_sec | 60.0 | 15.0 | 16:03 |
| ai_stale_data_max_age_sec | 60.0 | 15.0 | 16:04 |
| ai_watch_dead_seat_evict_sec | 90.0 | 30.0 | 16:05 |
| ai_watch_movers_min_rvol | 0.85 | 1.0 | with the volume-clock fix deploy |
| ai_entry_confirm_max_slip_px | 0.0 | decide (price-relative?) | tonight |
| ai_watch_engine_push_max | 44 | keep | — |

Reverts:
1. Edit the file on the MacBook `master-mac` branch.
2. Commit and push.
3. On the mini: `git pull --ff-only`. No restart is needed; config is read live.

**Claude's scheduled reminders will not reach you.** Do the reverts yourself after 16:00.

## Code in progress — branch `replay-tool` (pushed; NOT deployed)
1. `5cc38ff` `tools/rehearse_open.py`: an observed replay of a session's book, minute by minute.
   - Reads `ai_reports/admit_ledger/<day>.jsonl`, `decision_ledger/<day>.jsonl` and `events.jsonl`.
   - Shows per slot: seats, armable / gate / data-blocked, top refusals, trade path, and the pass criteria.
   - It reproduces this morning. Baseline: all criteria FAIL.
2. `22845f0` The volume-clock fix: `movers_screener.sip_data_mins_open()` and knob `ai_movers_sip_delay_min` (15).
   - Also `tools/rehearse_whatif.py`, which scores the fix. It adds 5 buyable names (BMNR, VZ, KR, PFE, CRCL) eligible from 09:34–09:51, with 8 arms against 3 live. They're slow large caps, so this is supply, not edge.
3. `765ca8a` The armable-only book:
   - `admit_arm_gates()` in `passes_inclusion` checks the price band for every source, plus the SIP spread and gap-down gates. It abstains on unknown data, and the spread check waits until 09:30 + delay.
   - Gate-blocked seats are evicted through `never_armable`. Fixed `_track_unarmable_block` clearing the clock.
   - Knob: `ai_watch_admit_arm_gates` (default off).
   - Projection with both fixes: median armable after 09:40 goes 1 → 6 (PASS). Book at 09:40 is 3, and the data-blocked share is 36% (both FAIL).

Full suite: 3608 passed, 1 xfailed.

How to run the replay tools on the mini without touching the live tree (untracked `.py` files in the repo make the tree dirty):
```
scp tools/rehearse_open.py tools/rehearse_whatif.py mac-mini-away:/tmp/
ssh mac-mini-away 'cd ~/repo/trading-helper && R=$PWD && PYTHONPATH=/tmp:$R:$R/tools:$R/tools/studies .venv/bin/python /tmp/rehearse_open.py --day 2026-09-24 --start 09:30 --end 12:00 --reports $R/ai_reports' < /dev/null
```
`rehearse_whatif.py` takes the same flags. It needs SIP data at least 15 minutes old.

## After-close plan, in priority order
**Tier 1: stability**
- (a) **Dashboard ticker list leak.** `transcription/wb_watchlist.json`: book rows (`src=book`) never leave. They're exempt from the age purge and the cap, so the list grows to 50+ over the day.
- (b) **Alpaca request budget.** Batch per-symbol quote calls, cache the SIP gate reads longer, pace the dashboard's startup quote burst (its first snapshot took about 2 minutes), and log requests per process.
- (c) **Watchdog check on dashboard snapshot freshness,** with auto-restart. Add the same check to `tools/live_check.py`.

**Tier 2: the book, which is the main bottleneck**
1. **ONE PRICE, ONE CLOCK.** Every book record takes the fresher of (engine `live_print`, dashboard quote) as its price and `price_ts`. All three freshness rules read that one age: arm (`ai_watch_decision_max_age_sec`), position flatten (`ai_stale_data_max_age_sec` in `ai_positions.py` around line 6210), and eviction (`ai_watch_dead_seat_evict_sec`).
   - Bug: `promote_stream_src_if_print_fresh` only consults `live_print` when `src != stream`.
   - Don't treat an Alpaca 429 as "no data". Keep the −50 cross state across evictions.
   - The main data blocker label is `tape_only` (and `stale_quote`).
   - Tests: CDNA, RKLB, ECO (printed 9.4s ago but labeled `stale_tape`).
   - Target: data-blocked share below 10% on the replay.
2. **Finish the armable-only book:** rank waiting candidates by arm proximity (fast %R approaching −50 and rising, tight spread, rvol_pace), and keep a short backup queue.
3. **Admission leaks:** `below_min_price` refuses 100–550 names every 5 minutes, probably priceless rows given the same label; `stale_tape_admit` refuses 100–450. Find out which.
4. **Scanner warm-up:** for the first 30 minutes, scan nominees keep the scanner's own IEX pct/price. Knob `ai_movers_scan_trust_min`.
5. **Fresher free data:** measure update intervals per source first. Then prioritize book names for Finnhub's 50 slots, and add the IEX quote stream between trades (for freshness only, not spread judgment).
6. **Confirm check:** don't cancel on favorable moves, and shorten the 7-second gap between arm and send.

**Tier 3: research**
- **Arm comparison** on the replay: −50 against −60, −70 and no arm, each with and without rvol_pace ≥ 1.64. Also concurrency: average and max positions open.
- **Is 09:30–09:45 profitable** net of spread?
- **Premarket scanner** rebuilt on delayed SIP.
- **More trending names on the book.**
- **Grok research prompt:** add the $20 floor.

**Tonight's order:**
1. Reverts after 16:00.
2. Tier 1 (a).
3. Tier 2 item 1 (one price, one clock), replay-verified.
4. Merge `replay-tool` into `master-mac`.
5. Deploy, with these in `bot_config`:
   - `ai_watch_admit_arm_gates: true`
   - `ai_watch_movers_min_rvol: 1.0`
6. Kickstart and verify:
   - `agy_auth=ok` in `logs/ai_trader.log`
   - snapshots rebuilding in `logs/dashboard.log`
   - engine `signal_state.json` fresh
7. Replay 09-21 through 09-24 against the pass criteria: book ≥ 10 by 09:40, ≥ 6 armable, data-blocked share < 10%, every arm reaches an order.

## Health checks during the session
- `ssh mac-mini-away 'cd ~/repo/trading-helper && .venv/bin/python tools/live_check.py' < /dev/null`
- **Frozen dashboard:** no new `[SNAP] rebuilt` lines in `logs/dashboard.log`, plus `[POLL] Read timed out` in `logs/engine.log`.
- **Hung process:** `sudo .venv/bin/py-spy dump --pid <pid>` gives the stack. The user must run it (sudo).
- **Hung engine:** the watchdog restarts it after 120 seconds of silence, with a 180-second startup grace.
