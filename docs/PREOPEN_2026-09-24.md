# Pre-open report — Thursday 2026-09-24

**Running:** commit `110e3fa`, config fingerprint `6c54d3c6302f`. Everything below is already deployed on the mini. The stack restarted at 16:30 ET on 09-23, AGY auth is intact, and the tree is clean.

## Bottom line

- **09-23 lost $61.78 on 37 trades (11% winners).** Entries bought after the move, exits sold fast, and trading cost about 0.2% per round trip.
- **Tomorrow runs a simpler desk:** one entry trigger, a $20 price floor, a stop that follows the peak, and a sell that can't fill far below the stop. Replayed on 09-23, this setup loses about **$20 instead of $62**.
- **It is not yet a profit machine.** The best setup tested (one-arm entries at $20+ with these exits) makes about +0.06% per trade before costs, consistent in both halves of the sample. Costs are about 0.11% per trade. To profit, costs must fall below about 0.06%, or entries need better information. **Tomorrow's first question is whether the capped sell brings exit cost down.**

## 1. The 09-23 scorecard

| Pillar | Measure | 09-23 | Target |
|---|---|---|---|
| Opportunities | Fills; hours with a fill | 37; 6 hours | A fill every hour |
| | Half hours with 10+ names on the book | 5 of 13 (only from 13:30, after the scanner went in) | All of them |
| Entry quality | Fills that went +1% before −1% | **41%** vs 59% for a random minute in the same names | Beat the random minute |
| | 30-minute return vs the random minute | −1.00 points (t = −2.9) | Positive |
| | Fills that never traded above the entry price | 43% | Low |
| Runway | Median best move within 30 minutes of the fill | +0.59% | Above +0.5% |
| | Fills that reached +1% within 30 minutes | 22% | |
| Capture | Share of that 30-minute runway we kept | **−59%** | Above 0 |
| | Exit price below the stop | −0.26% on average | Close to 0 |
| Bottom line | Net per trade · win rate · P/L | −0.41% · 11% · **−$61.78** | Above 0 |

The daily loss brake (3R) tripped at about 15:40.

## 2. What changed for 09-24, why, and the live check for each

| # | Change | Pillar | Evidence | Live check tomorrow |
|---|---|---|---|---|
| 1 | **One entry trigger:** buy when fast %R crosses up through −50 while slow %R is rising. Square, heating and the oversold triangle are off. | Entry quality | Entry screen, 09-14 to 09-22, counting only after a name was admitted: random minute 51.5%; **square 45.1% (z −2.6); heating 45.0% (z −4.1); −50 cross 51.0%** | Fills show `arm_why = mid_rise`; +1% before −1% at 50% or better |
| 2 | **$20 price floor** on the book, the movers seed and the scanner | Bottom line | Cost study, 953 fills: at $20+ the market moves +0.05% per trade and costs erase it (−0.06% to −0.07% realized), vs −0.20% at $10–20 and −0.48% under $10 | No fills under $20; book at 10+ names (watch for thinning) |
| 3 | **Stop follows the peak:** once up +0.3%, the stop stays 0.35% below the highest price | Capture | VKTX 09-23: +0.5% above the fill and still sold at −1%, because the stop followed a lagging median. Replay: better in both halves. | `shelf_trace` shows the stop rising with the peak; no "up 0.5%, sold −1%" trades |
| 4 | **60-second never-green rule off** | Capture | Exit sweep, 1,818 entries: the rule is worse in both sets and both halves (−0.02 to −0.04 points per trade) | No `no_progress` exits |
| 5 | **Capped sell:** when the stop hits, a sell limit 0.15% under the triggering print, then market for any remainder after 2s | Capture | Exit cost is 0.110% per trade, vs 0.058% for entries; 20% of exit fills landed below the lowest price of that minute | Trade log notes `collar_limit`; average exit fill vs stop better than −0.06%; count of fallbacks to market |
| 6 | **Full-market scanner:** snapshots about 11,000 symbols every 2 minutes; the movers band now $20–100 and +1.5% | Opportunities | 09-23 on a red tape (SPY −0.8%): the old feeds offered 3–5 names at $10+, while the full market had 51 green, liquid ones | `[movers] N rows (M from scan)` in `logs/movers.log`; book at 10+ from the open |
| 7 | **Live price streams:** engine and push caps raised from 32 to 44; watchdog restarts a hung engine; levered ETFs blocked | Opportunities / safety | The engine hung 13:51–14:15 on 09-23 with 50 subscriptions; SOXS got onto the book | Engine heartbeat stays under 120 seconds; the watchdog logs any `WEDGED` restart |
| 8 | **Three stop-seeding bugs fixed** (found by the test suite): a fresh fill's stop could sit above the market; a raised stop could be pulled back under the fill; the 30-second catch-up defaulted on | Capture / safety | Traced in tests; full suite 3,565 passed | No sell in the first seconds of a fill without a real print through the stop |

## 3. Counterfactual: 09-23 replayed under tomorrow's setup

Same names and bars; events only after each name was admitted; at most 5 open; one position per symbol; 2-minute re-entry cooldown; 3R brake; about $460 per trade; flat at 15:50. One-minute bars fill at the bar close, so this is somewhat optimistic.

| Version of 09-23 | Trades | Win % | Per trade | P/L at 0.11% cost | P/L at 0.20% cost |
|---|---|---|---|---|---|
| A. Actual | 37 | 11% | | **−$61.78** | |
| B. Actual entries at $20+, new exits | 23 | 26% | −0.23% | −$24.66 | −$34.29 |
| C. Square trigger, new exits | 32 | 53% | −0.14% | −$20.90 | −$34.30 |
| **D. Tomorrow's setup** | **46** | **52%** | **−0.09%** | **−$20.03** | −$39.29 |

Tomorrow's setup trades more, and through the whole day (09:40 to 14:14). Most of the improvement comes from the $20 floor plus the new exits. The size of the loss is set by the 8 of 46 trades that hit the full 1% starting stop.

## 4. Starting-stop test (why the 1% starting stop stays)

The sweep: starting stop 0.35 / 0.5 / 0.75 / 1.0% × trail 0.25–0.5%, on the real fills and the one-arm entries, at all prices and at $20+. **1% was the best starting stop in every set.** Tighter stops get hit by noise: 44–56% of trades take the full stop at 0.35%, vs 14–28% at 1%. The best setup is one-arm entries at $20+ with a 1% starting stop, at **+0.065% per trade before costs**, positive in both halves (t ≈ 2.4). Net that's −0.045% at 0.11% cost, and −0.14% at 0.20%.

## 5. What we learned on 09-23 (corrections included)

- **The names are a coin flip once admitted.** A random minute after admission goes +1% before −1% 51.5% of the time, with no drift. An earlier "56% and drifting up" figure was hindsight: it counted time from before the name was admitted, when it was already running.
- **No timing trigger beats a random minute.** Breakout, VWAP reclaim, VWAP pullback, opening-range breakout, higher lows, strength vs SPY and heating all come in at or below random. Opening-range breakout looked like a 74.6% winner until the same hindsight was removed.
- **No seed-time feature picks better names** than what the book already admits (53.5% vs 48.7%).
- **Exit shape can't make the average positive.** Every stop and trail combination nets 0 to +0.06% before costs. Exits decide the win rate, not the average.
- **Costs are the capture bottleneck:** market −0.05%, entry cost 0.06%, exit cost 0.11% per trade.

## 6. Tomorrow's live checks, and what to do if one fails

| Check | Passes if | If it fails |
|---|---|---|
| Arm fires | `mid_rise` entries appear by 10:30 | Look at `wait_mid_rise` / `mid_rise_stale` counts; the book may be missing low-%R names |
| Book size | 10+ names with EXH data most half hours | Lower the scan move floor (+1.5%) or the $20 floor, one at a time |
| Exit cost | Exit fill vs stop better than −0.06% on average | Raise the collar to 0.25%, or set it to 0 (plain market) |
| Peak ratchet | No trade up 0.5%+ that then exits below the fill | Check `shelf_trace` for that trade |
| Engine | Heartbeat under 120 seconds all day | The watchdog restarts it; check `logs/watchdog.log` |
| Daily result | Judged by config fingerprint at 30+ fills, not by one day | `tools/scorecard.py --by config_fp` |

**Rollback knobs, all live in `config/bot_config.json` with no restart:**
- **Entry:** `ai_watch_exh_mid_rise_arm: false` together with `ai_watch_exh_square_arm: true`.
- **Stop:** `ai_local_trail_peak_give_pct: 0`.
- **Sell:** `ai_exit_limit_collar_pct: 0`.
- **Price floor:** `ai_watch_min_price` / `ai_watch_movers_min_price` / `ai_movers_min_price` back to 10.

## 7. Still open

1. **Arming staleness guard:** refuse entries when engine data is more than 60 seconds old. The 13:51 hang let VKTX arm on frozen data. The watchdog now limits a hang to about 2 minutes, but the guard closes the gap.
2. **Spread gate:** VKTX paid about 0.46% in spread at entry. Check why the existing spread gate let it through.
3. **The percent-based arm knob** (`ai_local_trail_arm_pct`) is unsafe to turn on until `ensure_stop_behind_fill` understands it. A background task is queued; the knob is off.
4. **Better entry information.** Price, volume and %R timing carry no edge after admission. Next to test through the entry screen: catalysts and news, the daily trend, sector strength.
5. **Premarket scan:** the scanner starts at 09:35, so opening-bell names come only from the old feeds.

## Tools (all on the mini)

- **`tools/scorecard.py`:** the four pillars by day or config fingerprint.
- **`tools/runway_study.py`:** runway vs control, by day or version.
- **`tools/entry_screen.py`:** tests any entry trigger against a random minute.
- **In the scratchpad** (to promote if kept): `cost_study.py`, `exit_sweep.py`, `seed_sweep.py`, `counterfactual_today.py`, `name_quality.py`.
